# Project instructions

## 仓库形态与可移植性

### 当前阶段：可继续开发的完整源码仓库

- 当前远端仓库采用“换一台开发电脑也能继续工作”的源码仓库形态，而不是只保留服务器与客户端运行产物的精简发布包。
- 验收基线是：在一台新的、已获得必要账户权限的开发电脑上，clone 仓库并安装锁定依赖后，可以依据仓库内说明恢复开发环境，运行构建与测试，重建空数据库，启动本地应用，并在另行获得发布授权后部署目标环境。
- 应进入版本控制的项目自有材料包括：客户端与服务端源码、配置和依赖锁文件、完整数据库迁移链、运行构建所需的生成器或自动化脚本、测试及其配置与夹具、叙事作者源，以及无法由标准命令可靠重建的运行时资源。
- 不得进入版本控制的内容包括：密钥与本地环境变量、用户数据、数据库备份、日志、缓存、依赖安装目录、临时文件和可安全重建的构建输出。大型美术源文件若由外部资产库管理，仓库中应保留可追踪的清单或来源说明。
- `.gitignore` 应直接体现以上边界。不要先整类忽略迁移、脚本或测试，再为实际必需文件不断增加零散例外；发现此类结构时，应优先判断该目录是否属于可移植开发基线并整体整理。
- 新增功能时必须同时判断其源码、数据迁移、生成步骤、测试基础设施和运行说明是否能随 clone 一起恢复，不能只验证当前电脑或已经迁移过的 staging。

### 未来阶段：精简运行交付物

- 只有在产品进入成熟发布阶段，并由用户明确决定切换交付形态后，才制作只包含服务器、客户端和必要运行配置的精简交付物。
- 精简交付应优先作为独立构建产物、发布包或专用发布分支生成，不应直接删除开发主线中的作者源、迁移、生成器和测试历史。
- 切换前必须先定义目标平台、数据库初始化与升级方式、资源打包方式、版本与回滚策略，并验证精简产物可以从完整源码仓库重复生成。

## Notion 文档路由

- 本项目的 Notion 主界面是 [《伊萨卡手记》｜设计与开发](https://app.notion.com/p/3c2bb2de56728146ac1adee3b0db8198)。在本仓库相关对话中，当用户说“文档”或要求管理 Notion 内容、但没有指定具体页面时，先以该主界面为默认入口，再根据任务性质定位其中的相关页面或数据库。
- 当任务明确执行或适用 `research-through-design` skill 时，主要进入 [Research-through-Design](https://app.notion.com/p/Research-through-Design-3c2bb2de567281b7b14ae4b088e884ae)，并沿用其中已有的 Research Dashboard、研究问题演化、Design Episodes、AI-use Log 与研究协议等结构。
- Research-through-Design 页面是 RtD 研究档案的主要入口，不是普通产品设计、软件开发、进度管理或其他项目文档的默认入口；这些任务应从项目主界面寻找对应区域。
- 如果用户明确指定某个 Notion 页面、`README.md`、`docs/` 下的文件、其他名称或其他链接，则以用户明确指定的对象为准。
