# Client configuration

This directory owns declarative browser-side product configuration: scene definitions, default object rectangles, dialogue content, asset URLs, and time phases.

Rendering, interaction, persistence, and validation remain under `src/client/game/`. User-arranged object positions remain device state in `localStorage` and override—but do not modify—the defaults declared here.
