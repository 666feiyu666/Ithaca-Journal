# Server content

This directory owns authored narrative content such as letters and reflection prompts. Content modules are typed and validated against the product rules in `src/server/config/`; user progress and reading state remain in D1.
