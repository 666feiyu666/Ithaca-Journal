# Server configuration

This directory owns typed server-side product rules such as journey length and unlock behavior. Environment-specific deployment values and secrets remain in Wrangler configuration and environment bindings.
